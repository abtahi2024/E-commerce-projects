version: '3.9'

services:
  web:
    build: .
    command: python manage.py runserver 127.0.0.1:8000
    ports:
      - "8000:8000"
    depends_on:
      - redis

  redis:
    image: redis:7

  celery:
    build: .
    command: celery -A Backend worker -l info
    depends_on:
      - redis